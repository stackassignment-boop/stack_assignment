import MainLayout from '@/components/layout/MainLayout'

export default function RequirementDetailLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <MainLayout>{children}</MainLayout>
}
